query GetUserWithAllPosts($userId: Int!) {
  users(where: { id: { eq: $userId } }) {
    id
    name
    email
    created_at
    posts {
      id
      title
      content
      created_at
    }
  }
}
